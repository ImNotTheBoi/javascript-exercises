const pascal = function(n, arr = [1]) {
  //if one row or arr.length = n
    //return arr
  //for each num in array
    //rowArr varaible
    //divide array length
        //if below that num, add it to right num
        //if equal none
        //if above, add it to left num
    //if first or last number is not 1
        //add 1 and the first/last position
  //return pascal(n, rowArr)       
};
  

// Do not edit below this line
module.exports = pascal;
